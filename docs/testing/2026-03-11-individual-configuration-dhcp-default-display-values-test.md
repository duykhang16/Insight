# Test Note: Individual Configuration DHCP Default Display Values

## Expected Behavior

- wireless `IP Assignment` displays default values from Aruba `networksSummary` for:
  - Network Address
  - Subnet Mask
