# Test Note: Site Configuration Parent Child Highlight Behavior

## Expected Behavior

- on a configuration section route:
  - only the matching child item is highlighted
- when `Configuration` is manually expanded outside a configuration route:
  - only the parent row is highlighted
