# Test Note: Individual Configuration Initial Loading Gate

## Expected Behavior

- opening site `Configuration` first shows only the loading state
- the page does not briefly render the generic individual configuration header/template before the backend response is ready
- once loaded, the page renders the selected site network directly
