# Test Note: Individual Configuration DNS Static Empty Inputs

## Expected Behavior

- DNS `Automatic`:
  - fields are disabled
  - `-` is shown
- DNS `Static`:
  - fields are enabled
  - blank inputs are shown unless DNS values already exist
