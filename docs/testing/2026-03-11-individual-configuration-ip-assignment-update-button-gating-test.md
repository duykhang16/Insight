# Test Note: Individual Configuration IP Assignment Update Button Gating

## Expected Behavior

- static DNS mode with blank/invalid primary DNS disables `Update`
- invalid secondary DNS disables `Update`
- local wired-network mode with no wired selection disables `Update`
- valid form state enables `Update`
