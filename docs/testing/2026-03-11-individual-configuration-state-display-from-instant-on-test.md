# Test Note: Individual Configuration State Display From Instant On

## Expected Behavior

- load overview
- note current `State`
- toggle `Enabled`
- `State` label should remain unchanged before save
- after a successful refresh/save cycle, `State` should reflect backend data
