# Test Note: Site Configuration Never Highlight Parent

## Expected Behavior

- `Configuration` parent is never highlighted
- configuration child routes highlight only the matching subtask
- non-configuration routes highlight only the matching main item
- navigating to a non-configuration route collapses the configuration group
