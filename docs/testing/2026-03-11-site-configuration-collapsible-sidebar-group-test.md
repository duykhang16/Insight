# Test Note: Site Configuration Collapsible Sidebar Group

## Expected Behavior

- `Configuration` is visible as the main sidebar item
- clicking it expands the section submenu
- clicking it again collapses the submenu
- navigating directly to a configuration route auto-expands the group
