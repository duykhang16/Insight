# Test Note: Site Configuration Open To Overview

## Expected Behavior

- clicking collapsed `Configuration`:
  - expands the group
  - navigates to `configuration/overview`
  - highlights `Overview`
- clicking `Configuration` again while open:
  - collapses the group
