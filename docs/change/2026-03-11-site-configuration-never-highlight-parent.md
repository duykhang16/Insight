# Code Change: Site Configuration Never Highlight Parent

## Summary

The `Configuration` parent row is now a pure collapsible control and never an active-highlight target.

## Improvement

- eliminates parent/child highlight ambiguity
- keeps sidebar active state cleaner and easier to read
