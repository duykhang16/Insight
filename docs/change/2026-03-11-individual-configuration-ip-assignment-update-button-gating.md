# Code Change: Individual Configuration IP Assignment Update Button Gating

## Summary

The wireless `IP Assignment` `Update` button now disables when the current form state is already known invalid.

## Improvement

- prevents misleading clickable submit state
- aligns the UI with the existing inline validation and backend validation rules
